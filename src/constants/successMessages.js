import translate from '../locales';

export const ACCOUNT_VERIFIED = lang =>
  translate('This account is now verified', lang);
export const SUCCESSFULLY_REMOVED = lang =>
  translate('Successfully removed', lang);
export const SUCCESSFULLY_DELETED = lang =>
  translate('Successfully deleted', lang);
export const SUCCESSFULLY_MODIFIED = lang =>
  translate('Successfully modified', lang);
export const SUCCESS_TO_RESET_PASSWORD = lang =>
  translate('Check your email to reset password!', lang);
export const SUCCESS_PASSWORD_RESET = lang =>
  translate('Password changed successfully. Please continue to login!', lang);
